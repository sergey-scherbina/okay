- [ ] okay2-codec — `Schema` and JSON: the Schema enum (products, sums,
      options, lists, vectors, iso, bytes, bigint, defaults), its
      DERIVATION as a Scala 2 blackbox macro where Scala 3 uses Mirror +
      inline (the okay2 way: Distinct, Replayable), `Json` (the value,
      print, parse via JsonValue, strict read, mergePatch, encode/decode
      over Schema, read/write), and — after okay2-lex-parse — the lossless
      `cst`/`lossless`/`render`. XML is `okay2-xml`. Cbor/Edn/Yaml/
      Markdown/JsonSchema/Validate/Columns: on demand. (2026-09-25)
