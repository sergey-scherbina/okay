## backlog-audit-0923 - every open backlog entry checked against the code

The operator asked whether the backlog still matches reality
(2026-09-23). All 132 open entries across 28 sections were read and
every concrete claim checked with grep/ls: the files, tests, probes
and benchmarks they name exist (`ProbeGrowingOrder`, `MeasureRFrame`,
`Writer.expand`, `ChunkFlushBenchmark`, `People`/`Duration`/`Spans`,
`okay-langchain4j-embed`, `okay-flink`, `Conf.scheme`, the `ScanInto`
scanners — Markdown and Xml converted, Yaml and okay-rag's `Code`
not, as the entry says), and the recurrence ledgers are ledgers. What
did not match, fixed here: four CLOSED records sat in open sections
and move to refuted-declined-or-answered with their verdicts —
`handlers-fused-walk` (road 1, declared pointless in the roadmap's
Results after `Handler.flat` landed), `handler-fusion-step` (the arc
closed, `Fused` a test fixture), `item-058` (ANSWERED 2026-09-18, now
named `form-errors-on-validate`) and `ui-path-two-walks-answered`; two
entries named `item-NNN` get lane names (`spark-structured-streaming-
lane`); `jdk-internal-bad-symbolic-reference` is renamed
`cold-tasty-bad-symbolic-reference` as its own third occurrence
asked; `intent-second-author` was filed twice and is one entry;
`mrjar-jdk25-ci-gap` named `okay.script.api.Scoped` and
`okay-script/jdk25/` where the code is `okay-platform/.../Scoped.scala`
and the root `jdk25/`, and moves to `build`. Two entries whose
TRIGGERS ARE MET sat in the backlog and are promoted to the sprint
queue: `okay-x402` (okay-chain landed the same day the operator asked)
and `plutus-machine` (operator: "да оформи"). Left as they are, on
purpose: `ui-native-hosts-unread` (ui-gtk-integration is in flight and
will move its GTK gaps), `gate-watchdog-idle-sbt-cpu` (accurate:
gate.sh still counts the sbt JVM's own CPU), `widen-split` and the
day's new flake ledgers. Landed as 4af0d853.
