claimed: channel-queue-reversal — profiled okayChannelMerge (JFR): 30% CPU in Queue.dequeue's List.reverse, near-empty oscillating buf/receivers/senders defeats scala.immutable.Queue's amortization (batches never accumulate). Investigating a replacement data structure for Channel.State's Queue fields; measuring on an isolated micro-bench first before touching Channel.scala (core, used everywhere).
