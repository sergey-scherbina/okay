- optics-outside-topology — ANSWERED, not waiting: `okay.Tables.Plan[A]`
  already meets the staticness condition `Arrow` needs and has the
  three interpreters one would have been reached for (draw, optimize,
  compile) — the reasoning is in BACKLOG-ARCHIVE.md, `## optics-outside`.
