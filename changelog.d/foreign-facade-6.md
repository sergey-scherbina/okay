## foreign-facade-6 - docs/foreign-facade.md: one facade over every foreign language, with its examples pinned and its literature

Stage 6 of specs/foreign-facade.md. The page walks the facade as a user
meets it: a shop priced by whoever runs the module (`Road.value` over a
`JvmModule`, the same line over a `PyModule`), the data picking the road
(a value a call, rows one frame, a Flow frames — with the measured
costs), what a language cannot do not compiling, programs as data and
held objects through the same shape, and how a language is added. Every
Scala line is pinned by `TestDocExamplesForeignFacade`, which the
snippet map now names as the page's source; the JVM examples run in the
default gate. A README row, and the literature: Truffle's interop
protocol, Wadler & Blott's typeclasses, Raasveldt & Mühleisen, Arrow
Flight, Jupyter's kernel messaging, Erlang ports, Substrait, hexagonal
architecture. Stages 0–6 of the facade are landed; open beside it:
facade-frame-seam (the tier-2 seam), foreign-frame-op-rust-hs-go (their
`Frames` instances), R's and Arrow's cells of the table.
