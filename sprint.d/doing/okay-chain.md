- [~] okay-chain — new cross-built module, stages 0–2 of specs/chain.md: CAIP identifiers (Network/Account/Asset), Point/BlockRef/Finality, a sans-I/O `Tracker` (push sources) and `Poller` (poll sources), `BlockOf`/`Ledger`/`Movement`/`UtxoView`. ABSTRACTIONS ONLY: okay-watch's chain sources are not moved (operator 2026-09-23: whether they go public is undecided); `TestWatchShape` proves okay-watch can adopt it.
