- [~] loop-audit — specs/fold-until.md stage 2 rewrote 4 of the 12 hand-written `def loop(state)` programs (Toolkit) over `!.loop` and rejected 2 on reading (Actor receive, Dialog.run). The other six were never looked at: Ui.scala:693 (`loop(s, shown, rest)`), Form.scala:593/668/695, Conversation.scala:278, Nio.scala:97, chatweb Main.scala:30 — plus any written since (grep `def loop(`). Count the doors: rewrite the ones that read better as `!.loop` (state a value, `Right` the answer), record each rejection with its reason in the spec Decisions, suites unchanged and green.
