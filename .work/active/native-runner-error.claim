task: native-runner-error
agent: opus5-di
started: 2026-09-09
heartbeat: 2026-09-09T16:10:24Z
status: in-progress
branch: feature/native-runner-error
worktree: ../okay-wt-nativerun
spec: BACKLOG (the ComRunner entry) + gate-honesty

why: twice today a full matrix failed with `Error during tests` on a
  Native module and NO failed test — okay.codec.TestJsonEscape
  (110 of 116 run), then okay.lex.TestBpe (9 of 11). Both green alone.
  Each costs everyone a six-minute rerun, and a gate that cries wolf
  stops being read.

what: find what the runner actually reports (the two gate logs are
  kept), decide whether the loss is ours or sbt-scala-native's, then
  either fix it or make the gate say so honestly — and record the
  recurrences in the existing entry either way. A retry, if it comes
  to that, must NAME what it retried rather than hide it.

next: read the two logs for the runner's own words
