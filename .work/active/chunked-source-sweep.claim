task: chunked-source-sweep (one same-session StreamOps run: every library's chunked source beside its per-element lane; §5 rewritten from that one session)
agent: okay-42
started: 2026-09-07
branch: feature/chunked-source-sweep

why: BACKLOG chunked-source-sweep: §5's table mixes sessions (Staged from
  one, kyo Stream.range from another, ZStream.range and fs2 emits from
  the fairness audit's own run) with a ratio-to-floor caveat. Add the
  chunked lanes that live only in the fairness probe to
  StreamOpsBenchmark (ZStream.range, fs2 Stream.emits pure, and the
  staged pipeline if it is reachable from there), run the whole file in
  one session with -prof gc, and rewrite §5's table from that run with
  the per-element lanes beside the chunked ones. Benchmark and docs;
  no library code.
