- [~] form-drill — a form shown ONE LEVEL at a time (specs/form-drill.md; operator 2026-09-23 "будет" — a product with a deep record is coming): `Form.renderAt(value, path)` renders the focus's scalars as widgets and its composite fields/items as `<key>$into` buttons, in a DRILL MODE of the existing render algebra; `Form.drill[A]` is the screen (path pushed on `into`, popped on `out`, every other event through `submitted`); `drillValue` for an existing `A`. The cursor is the dotted PATH the router already routes by — not a zipper (Decisions). The typed zipper meets forms in `Form.askAt(cursor)` over `askFrom` (an `ask` seeded from a value). Law: edits through the drill with moves between them == the same events on the flat form.
