feature/persist-wire-repl 2026-09-01T19:10:00Z
agent: claude-code (session 75625ecc)
heartbeat: 2026-09-01T19:10:00Z
status: in-progress
next: replication's calls join the wire enum — produce (idempotent) +
  promote (operator failover) + compact, versioned to 2; the Server can
  host a Replicated coordinator (repl resolver); RemoteStore presents a
  Wire.Remote as a Store so Replicated holds a remote replica, machinery
  unchanged (replicate-pull = the existing Read). Spec box + tests.
files: okay-persist WireProtocol.scala, scala-jvm/Wire.scala,
  scala-jvm/RemoteStore.scala (new), tests; specs/persist.md
