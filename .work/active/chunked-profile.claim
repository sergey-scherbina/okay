claimed: chunked-profile — why the chunked per-element path (223us) is behind ZIO's (141) at the same chunk size, measured rather than guessed. The standing profile (71% inside the channel transaction) predates both chunking and the batched receive, which together cut that cost by more than an order of magnitude, so it no longer describes this path. Re-profile okayChunkedComposed with JFR, attribute every frame, and optimise what it actually names.
