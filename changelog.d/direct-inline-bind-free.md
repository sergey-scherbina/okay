## direct-inline-bind-free - refuted: a Free block's inline binds buy bytes, not time

The backlog entry after direct-staged: resolve a Free `direct` block's
binds to the given's `override inline flatMap` (the staged road's
precise val + `Select`) and measure. Built, with the two laws the entry
named — `ctxMonad`'s carrier kept on the quote road by making only the
Free carrier precise, the rebuilt program lambda quoted — and a third
the entry did not know: the inliner's receiver proxy re-homes a
`Free.delay` thunk (a reflected lambda) and LambdaLift loses its
`$anonfun`; held in a val first, 367/367 green. Then the number:
`freeDirectNested` 14.35 µs / 160 128 B against 14.25 / 164 928 —
bytes 0.97, time 1.00, under the 3% line; REVERTED, the quote road
stays byte for byte. What the ceiling lane (`freeHandNestedRun`, kept)
says instead: the hand-written Free program's edge (13.2 µs / 154 528 B)
is the DEFERRED SELF-CALL — a thunk and a `Delay` node per iteration,
the stack-safety feature — not the virtual bind. History `dib-*`;
backlog.d/refuted-declined-or-answered/direct-inline-bind-free.
