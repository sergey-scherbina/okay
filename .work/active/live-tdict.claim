task: live-tdict
agent: fable-persist
started: 2026-09-07
branch: feature/live-tdict
worktree: ../okay-wt-live

why: the cross-platform-state entry's second half, filed 2026-09-03
  "so the decision is made once, deliberately": okay-subscription
  moved onto TDict as a pure swap; okay-live's Hub/Registry did not,
  because the operator's ask named okay-subscription only. The two
  are a ConcurrentHashMap.computeIfAbsent and a CopyOnWriteArrayList.

what: Registry over TDict.computeIfAbsent, Hub over TList — the
  same semantics (one channel per key, publish to the current
  subscribers, no eviction), the module's last java.util.concurrent
  gone; the crossProject question decided and recorded (no JS/Native
  consumer named, the module stays JVM), the TDict "mk may run more
  than once" cost noted at Registry, where it is a discarded Channel
  on a lost first-use race.
