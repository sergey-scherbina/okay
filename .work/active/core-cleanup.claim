core-cleanup
owner: fable-5.1 session 53f400ad
started: 2026-09-15
what: the safe half of the Free/Cont/Effects review — remove
  TypeableK.unapply (no extractor use anywhere; two casts go with it),
  typeableKByClass (an alias nobody calls), fromFree/reflect duplicate,
  four copies of the reify handler; Effect.derived as one class; the
  orphan doc block over Free.resume; translate on split instead of
  <|>; Bind(Defer) rotation in one hop instead of two. The Delay node
  is a SEPARATE lane with a lane of its own (BACKLOG delay-node).
files: src/main/scala/Effects.scala, Free.scala, Cont.scala, Writer.scala,
  Instances.scala, Tag.scala, Refs.scala, specs/core-cleanup.md,
  BACKLOG.md, SPRINT.md, CHANGELOG.md
