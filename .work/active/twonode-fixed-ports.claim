feature/twonode-fixed-ports 2026-09-19T16:28:00Z
agent: claude-code
heartbeat: 2026-09-19T16:28:00Z
status: in-progress
done-so-far: worktree created; priced both fixes the entry asks for —
  ephemeral port + reading it back from the child (real work: stdout
  is currently redirected to DISCARD, the child would need to print
  its bound port and the parent parse it) vs the Live tag its two
  neighbors (TestChatDemo, TestRepoAgent) already carry. Taking the
  Live tag: matches precedent, correct per the entry's own framing
  (keeps a real distributed-failover test out of the default gate,
  the honest trade — not a bug being hidden)
next: tag TestTwoNode Live the same way TestRepoAgent does, land
