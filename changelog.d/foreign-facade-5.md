## foreign-facade-5 - the measurement table: every tier through the facade beside the language's own road

Stage 5 of specs/foreign-facade.md. `MeasureFacade` (Live) times tier 1,
2 and 3 through the facade beside the language's own road, medians of
five, load printed. On this box (load 4.9, python3 3.14 without
pyarrow, 100 000 rows): tier 1 0.217 ms against 0.130 ms — the codec
at `Schema` the own road skips; tier 2 188 ms against 137 ms — the seam
of two conversions each way, 37% worse than the own road and therefore
a defect by the spec's own rule, filed as facade-frame-seam with its
fix by road; tier 3 194 ms for the same rows in 25 frames — streaming
costs nothing over one frame. The JVM's call and by-reference frame are
a microsecond; its rows in and out 19.8 ms per 100 000. R's cells and
the Arrow cells wait for a runtime this box lacks.
