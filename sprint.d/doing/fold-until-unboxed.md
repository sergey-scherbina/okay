- [~] fold-until-unboxed — the last open item of specs/fold-until.md, after the operator removed the triggers ("все нужно", 2026-09-22): MEASURE whether a boxed `FoldUntil` on the primitive shape pays what `Fold` measured (45.2 vs 3.8 us per 10k Longs), then add `FoldUntil.OfLong` (and siblings) only if the number says so. Lanes beside `FoldBoxBenchmark` (compare, 10k Longs in chunks of 64): `Chunks.foldUntil` with a boxed `FoldUntil` whose stop never fires vs `Chunks.fold(Fold.sum)` boxed-data vs the `OfLong` data path, plus an `OfLong` FoldUntil prototype under a `foldUntil` dispatch. Record in src/jmh/history.tsv either way — a refutation is a result.
