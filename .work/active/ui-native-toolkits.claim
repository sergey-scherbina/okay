task: ui-native-toolkits
agent: fable-frontend
started: 2026-09-09
branch: feature/ui-native-toolkits
worktree: ../okay-wt-ui-native
spec: specs/frontend.md stage 3's deferred item; specs/ui.md "native toolkits (satellites over the same seam)"

why: the frontend arc's four stages landed (65320c76); the one leg
  left is a native toolkit as a HOST over the same Backend seam — the
  "everything Scala can do works out of the box" promise for a desktop
  window with no browser and no wire.

what: Swing on the JVM (zero dependencies, headless-testable): a patch
  Backend like Dom's — build components from the tree, keep a mirror,
  patch by path, delegate events by key — plus a window convenience.
  GTK via Scala Native only if the machine has GTK headers (checked at
  claim time; see the landing note).

predict: ~150 lines mirroring Dom.scala; the DOM law battery reused
  verbatim against a headless component tree.
