task: backlog-cleanup
agent: opus-optics
started: 2026-09-10
branch: feature/backlog-cleanup
worktree: ../okay-wt-backlog
spec: .agents/plugins/scrumban/commands/scrumban.md (the board rules)

why: the operator asked. 6 241 lines, 101 sections, 314 closed entries
  against 51 live ones — the flat-root-file failure mode the scrumban
  skill names, reached. Open items are misfiled (the lexer's two sit
  under `## okay-r`), section titles are themselves closed lanes, and
  the live list cannot be read.

what: BACKLOG.md keeps ONLY open work, grouped by the module that owns
  it, plus a one-line index of what was refuted so nobody retries it
  blind. Everything closed moves VERBATIM to BACKLOG-ARCHIVE.md — the
  refutations are the repository's memory and are not deleted. Every
  removed block is asserted present in the archive, mechanically.

not doing without a word from the operator: the skill's per-module
  <module>/BACKLOG.md split. It changes where every agent writes and
  wants AGENTS.md with it; offered instead.

gate: markdown only — the diff is asserted to touch no source file.
