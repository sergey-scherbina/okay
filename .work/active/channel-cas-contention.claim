claimed: channel-cas-contention — the item filed at the end of writer-of-resume-fix and channel-queue-reversal: profile TRef.modify's CAS-retry cost under Channel.merge's REAL feed/sch.fork fiber-contention shape (two genuinely racing Loom/Async fibers sending, one consuming), not a synthetic two-thread race. Find out whether retry count/rebuild cost, not the Queue data structure itself, is what actually dominates okaySourceMerge's remaining ~210us over the isolated Source.of floor.
