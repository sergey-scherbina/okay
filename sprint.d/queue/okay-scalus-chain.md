- [ ] okay-scalus-chain — new module okay-scalus (JVM): stages 0–1 of specs/scalus.md. scalus-cardano-ledger 1.2.0 as the ledger MODEL + CBOR codecs only; our own Ouroboros node-to-node transport (mux, handshake, chain-sync, block-fetch, keep-alive) on virtual threads; `Follower` with confirmation depth, parent continuity, shallow rollback absorbed / deep rollback surfaced; `Schema` instances for the ledger types. Traps already found: scalus `Block.hash` is the BODY hash (Block.scala:50) — compute the header hash from raw bytes; Byron is not modelled, start Shelley+. Fixtures = recorded relay sessions; live test against preprod relay tagged `Live`.
