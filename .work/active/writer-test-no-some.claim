task: writer-test-no-some
agent: fable-fusion
started: 2026-09-09
branch: feature/writer-test-no-some
worktree: ../okay-wt-wsome
spec: specs/handler-fusion.md (the floor), BACKLOG "runner-floor" item 1

why: Writer's own TypeableK tests the told value through
  scala.reflect.Typeable, whose unapply answers a Some per tell; every
  Writer runner pays it. Operator's order after the arc: this first.
what: a ClassTag-based class test where a ClassTag[W] exists (boxed
  class for primitives), Typeable as the fallback by given priority;
  measured on fusedSWr B/op (-16 B per tell expected, 333 tells);
  TestRowIdentity's two-Writers row must still misroute loudly.
