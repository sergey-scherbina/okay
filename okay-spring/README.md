# okay-spring

A Module's values as Spring singletons closed with the context, a Spring bean as a module, and a controller returning `A ! Async` served through WebFlux — wired by a Boot auto-configuration.

**Depends on:** `okay` (JVM), spring-context, reactor-core,

This page is a pointer, not a guide: the module's own doc
below carries the pieces, the decisions and the measurements.

## Further

| | |
|---|---|
| [`docs/modules/okay-spring.md`](../docs/modules/okay-spring.md) | what it is, and the reasoning |
| [`specs/di.md`](../specs/di.md) | the design and its decisions |
