- [ ] ts-one-shape — T1 of specs/typescript-types.md: the TypeScript worker speaks okay's JSON codec shape through a `Ts` API on the JVM, so ONE `Stubs.typescript` declaration serves all three scenarios (Scala backend + TS frontend, both in the browser, both on the backend). Operator (2026-09-23): "чтобы типы были каким-то волшебным образом одни и те же … чтобы их не приходилось писать руками два раза".
