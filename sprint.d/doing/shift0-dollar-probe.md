- [ ] shift0-dollar-probe — stage 0 of specs/shift0-dollar.md. TESTS
      ONLY, no machine change: the ICFP 2011 "A cat has Alice." example
      on today's `shift0`; λ$'s `v $ e` against `push(p)(e).flatMap(v)`
      for a 0-capture that drops `k` and one that calls it twice
      (expected to differ, watched failing, kept as stage 1's spec); and
      the answer to the spec's OPEN typing question (under-prompt
      captures to a `$` delimiter), taken from APLAS 2012's construction
      of shift from shift0 and `$`. (2026-09-24)
