- [~] gen-read-stop-residual — Gen.unfold.toList reads +40%%/+24 B over Writer.foldUntil on the same shape (generators-jmh, round4: 219.4 vs 157.3 us, both post-FoldUntil-as-data); the write-up named two candidate causes without separating them — "the Stop arm in every split" (the row's width) and "Free.delay per unfold step" (Writer's own prog ALSO delays per step, so this second cause is suspect on inspection). Decisive lane: Gen.of(prog) — the SAME Writer program widened by Stop via .plus[Stop], read through Gen.foldUntil (Chain/Xf.Id) — isolates the row-width cost alone from the unfold-specific shape. If Gen.of(prog).toList ~= Gen.unfold(...).toList, the whole residual is the Stop arm; if it ~= Writer.foldUntil, the residual is something in Chain/Xf/unfold's own delay, not Stop. NUMBER first, then a fix only if one is cheap (a non-inline TypeableK given, or split without Stop when the row is known Stop-free).
