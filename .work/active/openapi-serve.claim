task: openapi-serve
agent: opus5-di
started: 2026-09-11
heartbeat: 2026-09-11T10:10:48Z
status: in-progress
branch: feature/openapi-serve
worktree: ../okay-wt-serve
spec: specs/openapi.md stage 2

why: a document nobody serves rots, and the spec named two readers —
  a browser, and an integrator for whom the in-repo proxy is a
  COMMITTED document with a drift test (the shape okay-demo/deploy
  already has). Responses landed, so there is now something worth
  serving.

what: `OpenApi.routes(api, router)` serving `/openapi.json` and
  `/openapi`; the page RENDERED ON THE SERVER — no JavaScript, no CDN,
  because a page that needs the network is a liability in the
  air-gapped deployment okay-deploy renders for. Then the consumer: a
  real service's document committed and held by a drift test.

next: routes + page in okay-openapi, then the committed document
