- [ ] **clojure-effects-seqs** — okay-clojure, second pass (specs/clojure.md):
      a thin CLOJURE-side wrapper (`okay.core`, shipped as a resource:
      `perform`/`await`/`tell` building a program as data, and a monadic-let
      macro), walked by an okay driver — Clojure code performing okay
      operations, multi-shot; and Clojure lazy seqs <-> okay `Source`/`Chunks`.
      Operator, 2026-09-23.
