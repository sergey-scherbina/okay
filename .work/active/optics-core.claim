task: optics-core
agent: fable-frontend
started: 2026-09-09
branch: feature/optics-core
worktree: ../okay-wt-optics
spec: specs/optics.md (written by this claim), stage 0

why: the operator's direction (2026-09-09): lenses on profunctors.
  The repository has the parts (Monoid, Applicative over !, Schema's
  SIso/SProduct/SSum, Ui paths, State) and four consumers waiting;
  two prototypes settled the encoding (a nominal Optic whose
  constraint is a type parameter, composition as the intersection)
  and the field selector (a macro that only reads `_.f`).

what: specs/optics.md; src/main/scala/Optic.scala — Profunctor /
  Strong / Choice / Traversing, Optic with andThen as the meet, Iso /
  Lens / Prism / Affine / Traversal, the Function1 / Forget / Star
  interpretations, view / over / set / preview / foldMap / traverseOf,
  Lens.field[S]("name") typed by the Mirror; Focus.scala — the
  selector macro `Lens[S](_.f)`; the laws on all three platforms;
  the JMH gate (set via lens vs copy, over(each) vs map) with the
  1.5x bar the operator accepted; the macro policy in specs/codecs.md
  restated as "a macro only reads".

predict: the core is ~200 lines; the laws are the work; the gate is
  where the design meets the machine — if `first`'s tuple costs more
  than 1.5x of copy, optics stay a convenience layer and Ui.patch
  keeps its navigation (stage 2 decides by the number).
