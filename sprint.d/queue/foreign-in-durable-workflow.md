- [ ] foreign-in-durable-workflow — the operator (2026-09-24): "интегрировать
      все это в дюрабле и воркфлоу ... и в статические проц и ду нотейшен".
      Foreign workers (every language, every link, the stage 5 givens and
      stage 6's supervisor) used from okay's durable execution and
      workflow layers: `Durable` journals over `ForeignEval` (a foreign
      program's walk replayed without the far side; how that meets
      `SupervisedWorker`'s own replay), okay-persist workflows (`Wf`), the
      static `Proc` arrows (specs/static-workflow.md), and do-notation
      (`direct { }` blocks and for-comprehensions over foreign calls).
      First step: survey what already composes (`TestHaskellProgram`
      journals a Haskell walk already) and write the spec of what does
      not.
