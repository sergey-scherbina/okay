## foreign-facade-close — one job text over every language, and Speaks held to what the worker does (2026-09-26)

The last two Behavior boxes of specs/foreign-facade.md, and the
operator's foreign-facade item off the sprint.

- `FacadeConformance.job[M: Calls: Frames: Streams]`: a call, a frame and
  a stream written once; over python3 and R (the `okay-r-test`
  container's) it answers what it answers over the JVM, the module the
  only change, and against a module without `Frames` it does not compile.
- `FacadeConformance.agree`: a `Speaks` report compared with what is
  OBSERVED — by-reference is the same Table object back, Arrow keeps an
  empty column's kind, columnar JSON loses it (`Nulls(0)`); programs are
  run (`pairs`: four answers multi-shot, one one-shot, no instance none /
  in-jvm). A lie in either direction fails, each direction a test
  (`RoadModule` crosses by a real Arrow IPC write/read or a real
  `ArrowFrames` round trip, offline); a mutant dropping the frames check
  turned the suite red. A far-side `stream` claim fails until
  foreign-one-mux gives it an observation.
- Found: `TestRFacade` runs on this box — `TestR.rscript` falls back to
  the container's R through a shim; the spec said it skipped.
