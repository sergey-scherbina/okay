- [ ] scalus-executor-fetch — okay-scalus-spark's first version fetches every block on the DRIVER (the follower) and ships the bytes in partitions; for a BACKFILL (a stream started far behind the tip, or a batch over millions of blocks) the driver's single connection is the bottleneck. specs/scalus.md §6's original plan: partitions name block RANGES and each executor block-fetches its range from a relay (deterministic for confirmed blocks). Trigger: a backfill that is measurably driver-bound.
