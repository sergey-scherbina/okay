- [~] fold-until-stages-3-4 — specs/fold-until.md stages 3 and 4, triggers removed by the operator (2026-09-22, "все нужно"). Stage 3: `Stage.transduceUntil(z)(step: (S, I) => Stage[I, O, Either[S, R]], end: S => R): Stage[I, O, R]` in Pipe.scala beside `transduce` — a stage that stops PULLING when its step answers Right; plus `Take.foldUntil(using FoldUntil[W, S, R]): R ! Take % W`, the iteratee a FoldUntil is, written over `!.loop`. Stage 4: `foldUntil` on the `Foldable` trait (IterableOnce and Producer instances) with the `foldUntilTo` extension beside `foldTo`. Laws: a prefix parser stops the upstream (told count after the stop = 0), composes under `through` on both sides, ends honestly when the input ends first; `pipe(producer)(Take.foldUntil)` = `Writer.foldUntil`; `xs.foldUntilTo` = the List method. Tutorial/guide/typepedia name them; okay-direct Gen lane is told the sha.
