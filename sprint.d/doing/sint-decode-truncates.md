- [ ] sint-decode-truncates — `Schema.SInt` decodes through `.toInt` in CBOR (`Cbor.scala`, `in.intItem().map(_.toInt)`) and JSON (`n.toInt`, documented at `JsonStrict.number`): a wire value outside Int wraps or truncates silently instead of being refused. Same class as the SLong uint64 wrap fixed by schema-bigint (specs/codecs.md "Big integers"). Found 2026-09-23. Trigger: any SInt field carrying external data; decide whether refusal breaks a caller that relied on truncation (grep `SInt` decoders' callers first).
