task: router-doc-says-what-it-does
agent: opus5-intent
started: 2026-09-04
branch: feature/router-doc-says-what-it-does
worktree: ../okay-wt-rdoc

why: Router's class comment still says "Under the last margin nobody
  guesses — a person sees the candidates", which stopped being true in
  the same lane that wrote it: the last tier's floor is ZERO because
  its margin is meaningless, so with the model loaded nothing
  escalates. The paragraph that was supposed to say so never landed —
  a scripted edit matched no anchor and failed silently, and I did not
  check.

  Found while adding the register-shift numbers on top of it, which
  also did not land for the same reason.

files: okay-intent/src/main/scala/okay/intent/Router.scala
