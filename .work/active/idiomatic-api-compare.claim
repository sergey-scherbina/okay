claimed: idiomatic-api-compare — replace the forced chunkSize=1 comparison (methodologically off: it named an artificial mode, not either library's real API) with what each library's OWN idiomatic surface offers, verified from zio-streams' actual sources (not memory): ZStream.fromIterable(list) for sending a collection (DefaultChunkSize=4096), ZStream.unfold(s)(f) for an effectful step-by-step generator (verified: Chunk.single(a) per step, ZIO's real per-element path, not an imposed one), runForeach for element-at-a-time consumption (verified: ZSink.foreach over the same chunk machinery), runCollect/runFold for whole-stream. Mirror each with okay's most efficient equivalent (Source.of, Source.range, Drain-based read, toLazyList.foldLeft) and measure the four axes pairwise rather than one blended "elementwise" row.

--- taken over 2026-09-06 by opus5-intent (operator: "Бери доделай") ---
The original holder stopped on 3 Sep and never released this. Read
before touching anything: the WORK is done and in master. §6c of
docs/benchmarks.md carries all four axes paired, and the last row zio
led was closed on 5 Sep by fixing the producer (bufferChunked 90.4 vs
zioChannelForeach 135.6).

What was left uncommitted in ../okay-wt-idio is SUPERSEDED and was
dropped, not merged: a benchmark `okayChannelRunForeach` that master
already carries as `okayChannelForeach_elem_runForeach`, and a
history.tsv row measured BEFORE the pairing fix — landing that row
would put a known-mismatched pair back in the ledger.

So what remains is to close it honestly:
  - DONE 4ec35df5: three git conflict markers deleted from
    src/jmh/history.tsv, committed on master 5 Sep by the
    adaptive-parts merge. Both blocks kept; the file is append-only
    and never had a side to choose.
  - verify §6c's table reproduces before releasing a claim measured by
    somebody else — 17 lanes, @Fork(2). NOT to be run under load: the
    box was at 15.8 when this was written, so the run waits for quiet
    rather than producing a number that would need retracting.
  - release.
