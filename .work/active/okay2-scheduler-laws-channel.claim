slug: okay2-scheduler-laws-channel
agent: claude (session 6f505c09)
what: the two scheduler laws whose blocking device is a channel (TestSchedulerLawsChannel: cancel wins on a blocking send; adaptive survives a fiber blocking inside a worker), ported to okay2-stream over okay2-platform SchedulerFamily
