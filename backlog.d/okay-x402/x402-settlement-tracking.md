- [ ] x402-settlement-tracking — specs/x402.md stage 2b: follow a `SettlementResponse.transaction` with okay-chain's `Tracker` to the finality a server requires, instead of taking the facilitator's word. BLOCKED: needs an EVM chain source, and okay has none — okay-watch's is not to be moved while its publication is undecided (operator, 2026-09-23). Trigger: an EVM source lands in okay, or the operator decides okay-watch's. Applies to both transports (HTTP `Gate`, MCP `X402Mcp.gate`) through `Charge.settle`.
