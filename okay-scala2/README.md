# okay-scala2

okay from Scala 2.13: a facade written in Scala 3 whose public types a Scala 2 compiler reads through `-Ytasty-reader`.

This page only points to the real documentation. The module's own doc
has the setup, the example, and the measurements.

## Further

| | |
|---|---|
| [`docs/modules/okay-scala2.md`](../docs/modules/okay-scala2.md) | setup for a 2.13 build, the example, the reasoning |
| [`specs/scala2-facade.md`](../specs/scala2-facade.md) | why a facade and not a cross-build; stages |
