- [ ] free-return-rename — rename the answer NODE `Free.Pure(a)` to `Free.Return(a)` (operator, 2026-09-23: "Free.Pure переименуем в Free.Return — мне и раньше казалось что так будет лучше звучать"). The ROW `okay.Pure` (`A ! Pure`, = Nothing) and the lifting function `pure(a): A ! F` STAY — the collision this removes is exactly that three things named `Pure` met in one file (a test importing `okay.!.*` had to write `okay.Pure` for the row; memory `free-row-invariance-and-pure-names`). Mechanical but wide: every `case Pure(a)` match over `resume` (~42 sites, several modules — grep `Pure(` under `import okay.!.*`), `Cont.Pure` weighed for the same rename for symmetry (decide in the spec: the two trees name their return node alike today), the theory chapters (ch. 4, 11) and docs that print the tree's three shapes. Land under one gate; a warm worktree hides nothing here because it is a rename the compiler checks.
