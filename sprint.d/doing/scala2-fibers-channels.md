- [~] scala2-fibers-channels — fibers and channels for Scala 2.13 in okay.scala2 (specs/scala2-facade.md, later stages): `Fiber[A]` (`fork` of an `Eff[Async, A]`, `join`, `cancel`), `Channel[A]` (`send`/`receive` as `Eff[Async, _]`, `close`, `offer`), over the core's Scheduler/Channel. The core's `Channel` is a trait with plain methods — check first whether 2.13 can use it directly before wrapping. Operator, 2026-09-23 (same message as scala2-streams).
