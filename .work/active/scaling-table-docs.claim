task: scaling-table-docs
agent: fable51-core
started: 2026-09-12
branch: feature/scaling-table-docs
worktree: ../okay-wt-scalingdocs
spec: none — a documentation-only lane, no new measurement

why: operator, 2026-09-12 — "опубликуй это на гитхабе в документации
  про бенчмарки и везде где мы говорим о своей скорости - покажи эти
  цифры... покажи данные вот эти - эту таблицу". The core-scaling
  matrix (okay vs zio/kyo/fs2/java.util.stream/flink/plain-JVM at
  1/2/4/8 cores) already exists as raw rows inside docs/benchmarks.md
  §20's table (wroclaw-table-refresh, 2026-09-11); what does not exist
  is a DIGEST that reads as a scaling story rather than 38 rows sorted
  by throughput.

what:
  1. docs/benchmarks.md §20: a digest table (library x cores x ev/s x
     speedup-over-1-core) placed right after the big table, sourced
     from the SAME run — no new JMH invocation, this is a derived
     view, and it says so.
  2. README.md's "Fast" bullet (item 5) and the "Stream pipeline"
     table area: one added line naming the 5.3x-vs-1.6x scaling
     result, pointing at docs/benchmarks.md §20.
  3. push to origin, since the operator's word is "опубликуй на
     гитхабе" — this repo already publishes README.md and
     docs/benchmarks.md as its public face; v0.1.0 is tagged, this
     lands as ordinary commits after it on master.

not done: no new benchmark run. The numbers are the ones already in
  the table; this claim reformats and re-publishes them, and must not
  silently round or drop the 9% noise-floor caveat that sits beside
  the source table.

predict: docs-only, no compile, no test touches these files
  (TestDocsIndex reads docs/README.md's module list, not
  benchmarks.md's content) — checked before claiming. Gate is a
  sanity check, not a requirement for landing.
