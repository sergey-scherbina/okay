# okay-staging

Run-time staging: the staged codec for a `Schema` that exists only at run time. JVM only, its own module, optional by construction.

This page is a pointer, not a guide: the module's own doc
below carries the pieces, the decisions and the measurements.

## Further

| | |
|---|---|
| [`docs/modules/okay-staging.md`](../docs/modules/okay-staging.md) | what it is, and the reasoning |
| [`specs/codecs.md`](../specs/codecs.md) | the design and its decisions |
