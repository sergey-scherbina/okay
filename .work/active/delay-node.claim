delay-node
owner: fable-5.1 session 53f400ad
started: 2026-09-15
what: a Delay(thunk) case beside Defer, so a deferred call with nothing
  to do afterwards does not pay a rotated `.flatMap(pure)` tail down its
  whole subprogram (BACKLOG delay-node, specs/core-cleanup.md
  Decisions). Lane FIRST, measured on master before the node exists;
  controls fib*/relayPrebuilt/handlePrebuilt/statePara at -f 3; lands
  only if the controls hold. A refutation with numbers is a fine outcome.
files: src/main/scala/Free.scala, Cont.scala, Effects.scala,
  src/jmh/scala/okay/HandlerBenchmark.scala, src/jmh/history.tsv,
  specs/core-cleanup.md, BACKLOG.md, CHANGELOG.md
