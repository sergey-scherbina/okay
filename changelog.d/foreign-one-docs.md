## foreign-one-docs — one entry page for every foreign language (2026-09-26)

Stage 8 of specs/foreign-one.md, closing the arc. docs/foreign-languages.md
is the model on one page: the five things that cross (a value, a table, an
object, a stream, a program) and what picks the road; one program — price
lookup and discount, declared once as Scala callbacks — in Python, R, Go,
Rust, Haskell and Frege, run by the same Scala line over every link; who
carries what; the two runtimes (the wire's one engine, the JVM walker); the
wire's five operations and its transcript; adding a language in four steps;
literature (Truffle interop, Wadler & Blott, Plotkin & Pretnar, Armstrong,
Jupyter, Raasveldt & Mühleisen, Arrow C Data and Flight, Reactive Streams
and HTTP/2 flow control). Linked from README and docs/README; the seven
language and layer pages open with "a chapter of". Fixed on the way:
TypeScript already holds objects, so foreign-held-values names Go, Rust
and Haskell only.
