task: ui-protocol
agent: fable-frontend
started: 2026-09-09
branch: feature/ui-protocol
worktree: ../okay-wt-ui-protocol
spec: specs/frontend.md, stage 1

why: stage 0 (f99880f5) gave the tree its two levels; the wire still
  speaks the hand-mapped WireJson and has no handshake, so a client
  cannot claim a vocabulary and a Kotlin client has no contract to read.

what: derived Schema[Ui]/[Event]/[Patch] (closing the codec gaps that
  kept WireJson hand-written: Vector, recursion, defaults), JSON and
  CBOR from one definition, `hello {vocab, version}` as the first line
  in Wire.serve/client, docs/protocol/frontend.md rendered from the
  schemas, the conformance script (lines in, frames out) every host
  must reproduce. WireJson retires once the derived JSON equals it.

predict: the derivation gaps are the bulk; the hello is small; the
  rendered document is a test that writes a file.
