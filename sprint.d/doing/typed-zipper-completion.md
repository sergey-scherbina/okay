- [~] typed-zipper-completion — what specs/zipper.md stage 2 recorded as "not built" and the operator then asked for ("делать то что еще не построено", 2026-09-22): (1) the path BACK from a typed cursor as an optic — every frame keeps its `look` beside its `put`, and `asAffine: Affine[S, S, A, A]` composes them, the typed twin of stage 1's `Zipper.at`; (2) the TYPE-CHANGING cursor `TypedZipper.Poly[A, B, T]` — a focus and the function that puts a `B` back into a `T` (McBride's derivative applied to its hole), `down` by a four-parameter lens/affine/prism, `set(b): T`; no frames, no `up`, because after a type change "up" IS put-and-re-enter. NOT (3) `left`/`right`: fields are not a sequence, stays a Decision.
