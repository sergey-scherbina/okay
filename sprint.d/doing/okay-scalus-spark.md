- [ ] okay-scalus-spark — new module: stage 2 of specs/scalus.md (schema-bigint and okay-scalus-chain have landed). FIRST the `Schema` instances for scalus's ledger model (moved here from okay-scalus-chain: the Spark encoder is their only consumer); the source is okay-scalus's `ChainSyncSource`/block-fetch (its `CardanoBlock` keeps the exact bytes for the `cbor` columns). Generic `SparkSchema` (a `Schema.fold` algebra, in okay-spark) with the enum encodings of §4: pure enum → case-name string, sum → tagged sparse struct (`kind` + one nullable struct per case WITH fields — Parquet refuses empty structs), recursive (`Data`, `Timelock`, detected by the fold's `ref` back edge) → `cbor: binary` + `json: variant`, non-string-keyed maps → array<struct<key,value>>. DataSource V2 `format("cardano")`: batch + `MicroBatchStream` with confirmed-block offsets, partitions = block ranges fetched on executors; the §5 tables as SQL views over one derived `blocks` tree. PLAN (2026-09-23, operator: no Spark needed below the adapter, for okay-watch): spark-schema LANDED (ebdb6986); columns-neutral (Columns in okay-codec, SparkSchema a translation); scalus Schema instances; CardanoTables as typed rows in okay-scalus (Spark-free); then the DataSource here.
