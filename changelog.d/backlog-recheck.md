## backlog-recheck - six more closed entries, and the shape the sweep's rule missed

The audit an hour earlier moved 95 closed entries off the open board
by one rule: a first line of `- [x]`. The operator read the verdict
and said the data looked stale. Re-reading every one of the 92 that
remained against the files found six that were closed in PROSE and
still ticked open — the shape a first-line rule cannot see:

- `optic-law-rewrites` LANDED the same afternoon (the audit listed it
  as open work).
- `windows-int-key-panes` was REFUTED on 2026-09-11 by
  `windows-packed-key`, whose archived entry says the seam it asks for
  was written, measured in place and lost 9-13%.
- `gate-warm-warning-blindness` asked the gate to report how many
  compiles it looked at; it has, since the watchdog.
- `freer-base` and `freer-base-stage2` were one open item (stage 2)
  filed twice; the history stays in the archive.
- two entries whose first line says SUPERSEDED were on the open board
  because nobody ticked them.

All six are in BACKLOG-ARCHIVE.md with a dated closing line; the
audit's changelog entry carries the correction. 86 open entries
remain. The guard from the audit stands as it is: a rule for "closed
in prose" would be a rule for reading, and that is what this lane did.

Commits: this one.
