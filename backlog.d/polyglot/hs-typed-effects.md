- [ ] hs-typed-effects — the Haskell worker's `Prog` indexed by its effects (`Prog (effs :: [Type]) a` with a `Member e effs` constraint, as in freer-simple/polysemy), so GHC refuses a `perform` of an effect the program did not declare; operations typed by answer, as Frege's `Operation a` is. The TypeScript twin landed as ts-typed-effects (T12 of specs/typescript-types.md). Frege has no type-level lists, so there the typed answer is the limit.
