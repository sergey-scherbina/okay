- [ ] scalus-events-mode — stage 3 of specs/scalus.md: rollbacks as ROWS (`kind: applied | rolled_back`) in okay-scalus-spark. A range containing an orphaned block cannot be re-fetched, so the driver journals events (okay-persist, in the checkpoint dir) BEFORE offering them and offsets become journal sequence numbers. Trigger: okay-scalus-spark's confirmed mode running on mainnet, and a consumer that needs sub-`confirmations` latency.
