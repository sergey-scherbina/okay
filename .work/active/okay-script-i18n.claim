claimed: okay-script-i18n -- operator ask 2026-09-07: pages in several languages. Site(languages = Vector("en", ...)): the request's language is ?lang= (also set as the OKAYLANG cookie), else the cookie, else Accept-Language matched against `languages`, else the first; api.Lang.current. Page VARIANTS: page.<lang>.md beside page.md (index.uk.md), resolved for the current language with the base as the fallback -- routing, includes, forwards and login/error pages alike. STRINGS: i18n/<lang>.yaml (flat key: value, Meta.parseYaml) read by api.t(key, args*) with {0}-style placeholders, falling back to the first language and then the key; the i18n directory is never routed. The example store gets a Ukrainian index and header strings. Touches Site.scala, Api.scala, specs, docs.
