- [~] scala2-streams — streams for Scala 2.13 in okay.scala2 (specs/scala2-facade.md, later stages). The core's `Source[W]` is `Unit ! (Writer % W + Async)` — the SAME shape as `Eff[Writer[W] with Async, Unit]`, so a 2.13 `Source[A]` class can wrap it: constructors (`apply`, `unfold`, `range`, from an Eff that tells), `map`/`filter`/`take`/`chunked`, `runCollect`/`runForeach`/`runFold`, `merge` over a Channel. Every public method called from the 2.13 probe. Operator: "Стримы, файберы и каналы - нужно будет сделать их поддержку тоже" (2026-09-23).
