- [ ] okay-x402 — TRIGGER MET 2026-09-23 (okay-chain landed a4456e52; the operator asked the same day), promoted from the backlog by backlog-audit-0923 — specs/x402.md stages 0–1: x402 v2 types with derived Schemas (amount = SBigInt digit string, network = okay-chain CAIP-2 `Network`), base64 header codec with golden tests from x402's transports-v2/http.md, an okay-http 402 gate over a remote `Facilitator`, a client wrapper with a payment policy and a `Payer` trait (keys never inside). Operator 2026-09-23: "может быть нужна поддержка протокола x402". Trigger: okay-chain stage 0 landed and the operator asks for it.
