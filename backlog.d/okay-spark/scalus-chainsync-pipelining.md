- [ ] scalus-chainsync-pipelining — okay-scalus's chain-sync asks for ONE header and waits for it: measured 2026-09-23 (scalus-executor-fetch) at 43.7 s for 1 000 preprod headers, ~44 ms each, a relay round trip apiece — about half of a backfill in both `fetch` modes (1 000 blocks: 71–175 s). The chain-sync mini-protocol allows PIPELINING: send N `RequestNext` before reading the answers (the reference client does), which turns N round trips into ~one. Care: a pipelined `RollBackward` voids the requests after it; `AwaitReply` means the pipeline has reached the tip — collect what is outstanding before asking again. Test on the fake relay (answers in order, a rollback mid-pipeline), then re-run TestLiveExecutorFetch; the win should show in BOTH modes, and only then does `fetch = executor` get a fair comparison. Trigger: a backfill that is too slow, or the operator.
