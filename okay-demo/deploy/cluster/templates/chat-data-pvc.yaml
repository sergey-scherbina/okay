{{- $v := index .Values "chat" -}}
# generated by okay-deploy — edit the Deployment value, not this file
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Release.Name }}-chat-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ $v.dataSize }}
