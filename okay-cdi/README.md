# okay-cdi

A Module's values as CDI beans through a portable Extension — `@Named` by the plan, singleton, the closer run at the container's shutdown — and a container's instance as a module.

**Depends on:** `okay` (JVM), jakarta.enterprise.cdi-api 4. Weld SE is

This page is a pointer, not a guide: the module's own doc
below carries the pieces, the decisions and the measurements.

## Further

| | |
|---|---|
| [`docs/modules/okay-cdi.md`](../docs/modules/okay-cdi.md) | what it is, and the reasoning |
| [`specs/di.md`](../specs/di.md) | the design and its decisions |
