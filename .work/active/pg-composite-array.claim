feature/pg-composite-array — arrays of named composites
agent: claude-code (session 75625ecc)
heartbeat: 2026-09-02T00:05:00Z
status: in-progress
next: pg-composite-array-of-composite part 1 — type arrays whose ELEMENT
  is a named composite (addr[] -> Arr(Row(...))). Make cell decode an
  instance method decodeCell(oid,s) that consults the composites cache +
  a preloaded array-of-composite OID->elem map (pg_type.typelem where the
  element is relkind='c'); parseArray takes an element decoder so nested
  and composite elements type recursively. Table row-types (relkind='r')
  stay deferred (preload cost). Live TestPgComposite: array of a named
  composite, with NULLs. specs/sql.md box narrowed.
files: okay-pg PgSql.scala (decodeCell, arrayElemDyn, loadArrayElems,
  parseArray decoder), test; specs/sql.md
