# generated by okay-deploy from okay-script's Deployment value — edit the value, not this file
# what an operator overrides per install lives HERE; everything
# else is in the templates, concrete, where it can be read.
web:
  image:
    repository: "okay-script/web"
    tag: "local"
    pullPolicy: IfNotPresent
  replicaCount: 1
  resources: {}
  dataSize: "1Gi"
