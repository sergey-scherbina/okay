- [ ] **frege-sbt-plugin** — project/Frege.scala as a published SOURCE sbt
      plugin (`okay-frege/sbt-plugin`, the okay-deploy-sbt precedent): an
      AutoPlugin a user of okay-frege enables to compile their own `.fr`
      (forked, `-target 17`, warnings fail); the repo's own build uses it the
      same way. Operator, 2026-09-23.
