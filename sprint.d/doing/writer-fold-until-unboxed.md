- [~] writer-fold-until-unboxed — the last unmeasured corner of specs/fold-until.md: `Writer.foldUntil` and `Producer.foldUntil` do not dispatch on the unboxed `FoldUntil` shapes because "the tree step dominates" — a guess, not a number (the operator removed the triggers on this spec). MEASURE on `FoldUntilBoxBenchmark`'s shape: a 10k-Long writer program read by `Writer.foldUntil` with a generic `FoldUntil` vs an `OfLong` one (the trait exists; the walk ignores it), and a local prototype walk with `addLong`/`doneLong`; per-lane gated runs, `-prof gc`. If the box is a measurable share of the tree step, add the dispatch to both walks; if not, record the refutation and close the corner.
