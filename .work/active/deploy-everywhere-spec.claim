claimed: deploy-everywhere-spec -- the operator's ask (2026-09-07): make deployment and configuration declarative and portable -- laptop, rented host, cluster, PaaS, AWS/Azure/GCP -- with answers taken: a FULL dependency model (their call, over my warning), configuration as a value with defaults plus a file over it, and secrets reaching cloud managers and SOPS. SPEC ONLY this pass: specs/deployment.md -- the model (Deployment/Service/Need/Settings/Secret), targets as pure renderers of files, one driver that shells out to each target's own tool, the staging that makes it landable, and an explicit list of what the model cannot say plus the escape hatch for it. No code, no build.sbt.
