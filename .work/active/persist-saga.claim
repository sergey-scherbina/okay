task: persist-saga
agent: fable-persist
started: 2026-09-09
heartbeat: 2026-09-09T06:54:59Z
status: in-progress
branch: feature/persist-saga
worktree: ../okay-wt-persist-saga
spec: specs/persist.md, new section "The saga"

why: persistence audit lane 6 of 7: the multi-item change is spec'd as
  a journaled sequence of conditional writes, and nothing packages it.

what: okay.persist.Saga[S]: steps with a compensation each, intent
  journaled before each step on a keyed topic (Typed envelope, state as
  CBOR at the edge), recover() folds the tail and finishes forward or
  compensates back by a declared Policy, status as a Schema value.
  Tests on MemoryStore: happy path, failure + compensation, crash
  window under both policies, recover idempotent on a finished saga.

next: worktree, Saga.scala, TestSaga (JVM)
