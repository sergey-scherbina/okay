feature/pg-sslmode 2026-09-01T22:55:00Z
agent: claude-code (session 75625ecc)
heartbeat: 2026-09-01T22:55:00Z
status: in-progress
next: the pg driver speaks sslmode (specs/tls.md open box, pg lane). The
  SSLRequest dance lives IN the driver, the TLS session comes from the
  seam. Factor PgSql.connect -> connectOver(conn: NetConn) (shared, no tls
  ref); add JVM-only PgSql TLS connect: raw socket -> SSLRequest (code
  80877103) -> read 'S'/'N' -> on 'S' Tls.client wraps -> NetConn over the
  SSLSocket -> connectOver. okay-pg.jvm compile-deps okay-tls (JVM only;
  the box's own shape). Enable ssl=on on the docker pg (reloadable, plaintext
  still works) to verify Require over TLS live; a negative for 'N'-refusal
  shape. specs/tls.md pg box + Results.
files: okay-pg PgSql.scala (connectOver factor), scala-jvm/PgTls.scala (new),
  build.sbt (okayPg jvm dep okayTls), scala-jvm test, specs/tls.md
