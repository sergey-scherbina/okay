task: intent-extract-amount
agent: fable-persist
started: 2026-09-07
branch: feature/intent-extract-amount
worktree: ../okay-wt-amt

why: intent-extract-more-slots names amounts as "a Slot.extract
  rather than a design" — the third parser after Duration and People,
  the same shape: total, deterministic, number words shared through
  Numbers, the fixture's languages.

what: Amount.parse/find — a number (digits with . or , decimals,
  thousands separators, or number words) beside a currency (symbol
  before or after, ISO code, the currency's name in the language,
  inflected: dollars, евро, гривень, złotych, 円), Amount(value:
  BigDecimal, currency: String) with the currency normalised to ISO;
  `Slots.amount`; tests per language; specs/intent-classify.md
  Results.
