# generated by okay-deploy from demo-chat's Deployment value — edit the value, not this file
# what an operator overrides per install lives HERE; everything
# else is in the templates, concrete, where it can be read.
chat:
  image:
    repository: "demo-chat/chat"
    tag: "local"
    pullPolicy: IfNotPresent
  replicaCount: 1
  resources: {}
  dataSize: "1Gi"
