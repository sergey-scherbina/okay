- [ ] bind-in-row-union — a bind whose two sides sit in DIFFERENT rows, answered in their union: `extension [A, F[+_]](p: A ! F) def bindIn[B, G[+_]](f: A => B ! G): B ! (F + G)` — both sides lifted through `RowLift.In` (the witness, one commented cast, NO tree walk), so `Reader.ask[Int].bindIn(e => Writer.tell(...))` typechecks where `flatMap` refuses (Free is row-invariant by a measured decision, specs/writer-covariance.md; the ergonomics is what is left). Operator 2026-09-23: "не возражаю — делай. Только еще подумай как можно это будет еще улучшить с точки зрения использования и эргономики". The thinking to do before the spec: (1) is `bindIn` the primitive or is it `p.plus[G].flatMap(f.andThen(_.plus[F]))` written once — if the latter, ship the SPELLING, not a fourth combinator; (2) a `for` over mixed rows: `map`/`flatMap` cannot widen, but a `given` `Monad` over `[A] =>> A ! R` for a NAMED row `R` lets `for` work when every generator is `.at[R]` — measure whether a row-polymorphic flatMap (`flatMap[B, G](f: A => B ! G)(using In[F, F + G], In[G, F + G])`) can REPLACE Free's own flatMap without a cost at the `Pure` row (it must not: flatMap is the hottest path); (3) the direct block already does all of this — say in the guide when to reach for which. Law: `bindIn` equals `flatMap` after two `at`s, byte for byte on the tree; B/op equal to `at`'s (no walk).
