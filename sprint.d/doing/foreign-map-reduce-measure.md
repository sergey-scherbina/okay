- [ ] foreign-map-reduce-measure — the number foreign-map-reduce landed
      without (operator's plan, 2026-09-25, item 5): the map in Python
      against the map in Scala on 1M rows, over the JSON road and over
      Arrow (a python with pyarrow), and the reduce in Python against
      `Wire.fold`; medians, box load beside, recorded in the spec and
      docs/modules/okay-cluster.md. `MeasureForeignMapReduce` (Live).
