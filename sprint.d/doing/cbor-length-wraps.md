- [ ] cbor-length-wraps — `Cbor.In` reads a text/byte-string length as a Long and passes `n.toInt` to `take` (Cbor.scala `textItem`, `byteStringItem`, both `skipHere` arms): a declared length of 2^32+5 wraps to 5 and reads five bytes as if that were the item, desynchronising everything after it instead of refusing. Negative wraps are already refused by `take`. Same class as sint-decode-truncates (a wire integer narrowed silently); found 2026-09-23 while fixing it. Fix: refuse a length past Int.MaxValue (or past the bytes left) before `take`; a test with a hand-built head `5b0000000100000005`.
