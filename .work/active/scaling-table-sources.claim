task: scaling-table-sources
agent: fable51-core
started: 2026-09-14
branch: feature/scaling-table-sources
worktree: ../okay-wt-scalingsources
spec: none — adds links beside prose that already names these paths

why: operator, 2026-09-14 — asked in chat to find and show links to
  the source behind the core-scaling table, then "добавь эти ссылки в
  документ про бенчмарки". §20 already narrates every one of these
  paths in prose (`compare/src/main/scala/okay/wroclaw/`,
  `scripts/wroclaw-bench.sh`) but as inline code, not as links a
  reader can click through on GitHub.

what: a short "Source" table right after the existing "shared half...
  lives in compare/src/main/scala/okay/wroclaw/" sentence, one row per
  lane in the table (okay, plain JVM, java.util.stream, fs2,
  zio-streams, kyo, flink), each linking its Bench (what
  scripts/wroclaw-bench.sh actually runs) and Lane (the logic) file —
  the same seven pairs verified in chat against the driver script.
  Relative markdown links (`../compare/...`), not GitHub URLs pinned
  to a commit: this doc lives in the repo and should point at the
  CURRENT file, the way every other cross-reference on this page
  already does with plain paths.

verified before writing: every one of the fourteen files exists at
  the path scripts/wroclaw-bench.sh actually invokes (checked in the
  chat that preceded this claim, re-checked here).
