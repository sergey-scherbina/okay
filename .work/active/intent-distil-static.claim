task: intent-distil-static
agent: fable-persist
started: 2026-09-07
branch: feature/intent-distil-static
worktree: ../okay-wt-dstat

why: intent-distil-more's second half — "worth trying on the STATIC
  table, which was not fed here because its vocabulary would have to
  be re-embedded, a second pass over the teacher rather than a change
  of method". The first half (more generated rows) is machine time
  against a verdict intent-distil-dose just sharpened: the dose's gain
  was one split's. This is the second pass.

what: TestStaticDistilled (Live): one distillation of every unit in
  the human fixture and the 182 self-consistent distilled rows; the
  best table so far (words+pairs+triples at PCA 256) fed the corpus
  at each of its inputs in turn — VOCABULARY, then WEIGHTS, then 20
  TRAINING rows — on both mirror splits, the human-only table as the
  baseline in the same run. Conditions printed; a default moves only
  on a gain that clears the bar on both splits.
