claimed: writer-of-resume-fix — Writer.of's recursive unfold wraps EVERY element in a redundant pure(()).flatMap (Bind(Pure,k)) that resume must rotate away (profiled: 46/120 CPU samples in resume's rotation cases, JFR on okaySourceMerge); the wrapper is only needed ONCE at the top, recursive calls are already deferred by the enclosing flatMap. Fix: split into of (one wrapper) + private ofLoop (no re-wrap per element).
