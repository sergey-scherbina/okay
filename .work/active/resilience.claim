task: resilience
agent: fable-resilience
started: 2026-09-09
heartbeat: 2026-09-09T05:39:01Z
status: in-progress
branch: feature/resilience
worktree: ../okay-wt-resilience
spec: specs/resilience.md (to be committed first), stage 0

why: the operator's microservices direction (2026-09-09): the audit
  found retry/timeout/tracing/health/auth/durable execution present and
  circuit breaker, bulkhead, rate limiter, hedging and deadline
  propagation absent (0 hits for breaker/bulkhead/limiter). One spec,
  five handlers around one operation, state as values for /metrics.

what: new module okay-resilience (JVM+JS, depends on okay-http):
  Breaker, Bulkhead, Limiter (token bucket, keyed), Hedge, Deadline —
  each a program transformer over A ! Async with an injected clock so
  tests are deterministic; stage 1 composes them around trait Http in
  a fixed order and renders their stats in okay-ops.

next: write and commit the spec; then stage 0 in the worktree
