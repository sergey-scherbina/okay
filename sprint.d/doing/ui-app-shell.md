- [ ] ui-app-shell — okay-watch operator 2026-09-24: the desktop app must
      look and behave like an app, and that belongs in okay-ui, not in a
      product's patches (*«через использование okay-ui?» … «да сделай»*).
      okay-ui's HTML host gains: `Shell` — an application frame (brand,
      a sidebar of grouped links, the page's place in it, the body beside
      it) for plain and live pages alike; `Enhance` — the page behaviour
      an app needs over plain HTML: a dropdown that opens under its field
      at its width in the page's font (not the engine's small list), a
      press sent in the background with a spinner beside its button and
      only the content replaced (the scroll stays), a page that keeps
      itself fresh by fetching instead of reloading, and on the live road
      a spinner on a pressed button until the patch comes. specs/ui-app.md.
      (2026-09-24)
