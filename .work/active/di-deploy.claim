task: di-deploy
agent: fable-di
started: 2026-09-09
heartbeat: 2026-09-09T07:58:25Z
status: in-progress
branch: feature/di-deploy
worktree: ../okay-wt-dideploy
spec: specs/di.md stage 3 (BACKLOG "di": di-deploy)

why: a Service says its database in the deployment and the module says
  it again in its type; the spec's stage 3 makes the type the one place.

what: okay-deploy `Needs[A]` (a capability type's Need, a given the
  application declares) and `Needs.of[Root]`, a macro walking the root
  module's unresolved inputs (`Pg ?=> Files ?=> Module[…]`), one Need
  per input, a missing given a compile error. Tests; docs/modules/
  okay-deploy.md paragraph; specs/deployment.md "The model" pointer.

next: write Needs + tests, okayDeploy/test, then the matrix
