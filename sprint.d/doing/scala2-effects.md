- [~] scala2-effects — a Scala 2.13 user's OWN effect in okay.scala2 (specs/scala2-facade.md, stage 3): ops extend `Op[+A]`, `object X extends Effect[X]` (ClassTag test in place of `derives Effect`), `Handler[F, R, B]` gets the op AND the continuation. Prototype measured from 2.13 on 2026-09-23: Console (resumptive) and Choose (multi-shot, all 4 answers). Open: `R = Any` inferred at the last handler trips -Xlint. Operator: "свои эффекты в скала 2 нужно будет подумать как сделать".
