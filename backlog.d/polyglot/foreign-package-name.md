- [ ] foreign-package-name — the engine every wire language shares lives in
      package `okay.py` (module okay-py), a name from when it served Python
      alone (foreign-names renamed the types, not the package). A move to
      `okay.foreign` (module okay-foreign) with `okay.py` kept as aliases
      for a release; hundreds of imports and the docs move with it, no
      behaviour. Deferred from foreign-one-runtime as pure naming.
