claimed: okay-script-serve -- operator ask 2026-09-07: one line to run a Site. Site.serve(port): Server ! Resource = Jetty.serve(port)(routes)(ws, push), so okayJetty becomes a MAIN dependency of okayScript (it was Test-only for the lifecycle proof); and a stock entry point okay.script.Serve (main(dir, port), OKAY_DATA=<dir> -> Sessions.persisted + Application.persisted over a FileStore, prints the URL, releases on interrupt) so a directory of pages runs with no code of its own: sbt "okayScript/runMain okay.script.Serve pages 8080". Touches build.sbt, Site.scala, a new Serve.scala, specs, docs; a Live test over Site.serve(0).
