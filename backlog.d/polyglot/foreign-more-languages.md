- [ ] foreign-more-languages — `Language` instances (and module types) for
      TypeScript, Haskell, Go and Rust in okay-foreign-cluster, so their
      functions run as cluster stages and through the facade as Python's
      and R's do (foreign-one-runtime, 2026-09-26, Decision 16: one small
      object each — a pool opener, an address, `Shape.python`/`json`).
      Trigger: a caller who needs one (the table road in Go, Rust and
      Haskell landed with foreign-one-bulk). Gate: `FacadeConformance` over
      each.
