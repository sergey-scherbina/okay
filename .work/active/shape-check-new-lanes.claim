task: shape-check-new-lanes (the rule written where a new lane's author reads: benchmarks method section + AGENTS)
agent: okay-42
started: 2026-09-07
branch: feature/shape-check-new-lanes

why: BACKLOG shape-check-new-lanes is a RULE, not work: every competitor
  lane built by foldLeft gets a right-nested twin before its number is
  quoted (kyo Env/Emit/Resource: the foldLeft shape is O(N^2) in kyo,
  ~1000x, and was read as the library's price for a week). A rule that
  lives only on the board is not applied. Write it in docs/benchmarks.md
  where the method is stated, in AGENTS.md's benchmark rules, point at
  the ReaderBenchmark twin as the example, and close the item. Docs only.
