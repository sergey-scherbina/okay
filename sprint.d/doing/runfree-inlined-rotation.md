- [~] runfree-inlined-rotation — the untried idea: `runFree` keeps its own inlined rotation while `resume` stays as it is (a third copy rather than a smaller shared one). First attempt (shrinking resume itself) REFUTED 2026-09-15 (409c06e2): every lane got worse, because resume is a loop and inlining a loop into callers that are loops nests loops. NUMBER first: a runFree variant with its own local rotation loop, alternated against the unmodified runFree on the same lanes (effCont24, nestedSWr, relayPrebuilt, fusedSWr, handleForward) before touching resume itself.
