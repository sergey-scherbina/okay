task: di-tails
agent: fable-di
started: 2026-09-09
heartbeat: 2026-09-09T10:16:50Z
status: in-progress
branch: feature/di-tails
worktree: ../okay-wt-tails
spec: specs/di.md stage 2 leftovers (operator's go, 2026-09-09): CDI bridge, WebFlux end-to-end

why: the arc closed with two things documented and not built; the
  operator asked for them.

what: new satellite okay-cdi (jakarta CDI 4 API; Weld SE in tests):
  OkayCdi.extension(m.exports) — one synthetic ApplicationScoped bean
  per export, @Named by the plan, typed by the erased class, the
  closer destroyed with the container; OkayCdi.instance[A]. okay-spring:
  a WebFlux test through the real handler stack (WebTestClient
  bindToController) in the default gate, and a real Netty server on a
  random port under the Live tag. build.sbt appended + okay-spring test deps.

next: rebased on e7bba6fe, one clean commit ef302b0b; waiting for the box (67MB free) to re-run docs-index + spring + cdi suites, then ff-merge
