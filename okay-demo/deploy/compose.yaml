# generated by okay-deploy from okay-demo's Deploy value — edit the value, not this file
services:
  demo-chat:
    build:
      context: ../..
      dockerfile: okay-demo/deploy/Dockerfile
    image: okay/demo-chat:local
    ports:
      - "8090:8090"
    environment:
      OKAY_CHAT_PORT: "8090"
      OKAY_CHAT_LOG: ":memory:"
