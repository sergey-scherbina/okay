claimed: intent-second-embedder — UNBLOCKED: Qwen3-Embedding-4B is now served alongside the 0.6B, 2560 dimensions against 1024 and genuinely different vectors. This is the experiment intent-embedding-choice could not run, and it is the one that could move the ceiling rather than the classifier: the journal already showed the probe and the model tier sharing ZERO errors, so the signal is in the text and the vector is losing it. Re-run the probe, the centroid and the per-language table on both embedders, same splits, same code, so the only thing that differs is the vectoriser. Also re-run the static distillation on the better teacher, since a static table inherits whatever its teacher knows. Three outcomes worth having: the ceiling moves and the no-model target is nearly met; only Russian moves and the gap was multilingual quality; nothing moves and 88.3% is this TASK rather than this vectoriser.
