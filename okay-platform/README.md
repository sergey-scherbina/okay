# okay-platform

The concrete JVM, JavaScript and Native runtimes for `okay-async`'s effect, plus the system facades built on them — `CanBlock`, the schedulers, `Net`, `Scoped`.

This page is a pointer, not a guide: the module's own doc
below carries the pieces, the decisions and the measurements.

## Further

| | |
|---|---|
| [`docs/modules/okay-platform.md`](../docs/modules/okay-platform.md) | what it is, and the reasoning |
| [`specs/cross-platform-async.md`](../specs/cross-platform-async.md) | the design and its decisions |
