claimed: merge-chunk-param — fold mergeChunked back into merge as `chunk: Option[Int] = None`, beside the capacity knob it resembles. Discoverability is the point: the session's finding is that callers of merge silently pay 5x, and a sibling method only helps those who already know it exists. Option rather than Int: Some(1) is NOT None (a one-element chunk still allocates per element), so the choice is genuinely binary, not a size starting at 1. Same numbers expected; mergeChunked removed, nothing released yet.
