- [~] scala2-facade — okay from Scala 2.13 through a facade module (okay-scala2, Scala 3 source, API free of inline/union/opaque) read by 2.13's TASTy reader; stage 1 is `Prog` over Async + Throws % Throwable plus okay-scala2-probe, a 2.13 project compiled with -Werror in the gate. specs/scala2-facade.md. Operator: facade over cross-build (2026-09-22, after the measurements in the spec).
