task: wroclaw-benchmark-page
agent: fable51-core
started: 2026-09-14
branch: feature/wroclaw-benchmark-page
worktree: ../okay-wt-wroclawpage
spec: none — a curated standalone page, sourced entirely from
  docs/benchmarks.md §20's already-verified content

why: operator, 2026-09-14 — "Сделай отдельную страницу по этим
  бенчмаркам стримов где укажи всю необходимую информацию - результаты
  в виде таблицы, ссылки на исходные коды и все необходимые объяснения
  и пояснения". §20 is 1132 lines deep inside a 4000+-line page; a
  reader asking "what does the streams benchmark show" has to find it
  first. docs/existentials.md is the precedent for this shape: a
  standalone page for one measured story, linked from docs/README.md's
  "Going deeper" table and from README.md, with the exhaustive version
  staying where it is.

what: docs/wroclaw-streams-benchmark.md — curated, not a copy:
  1. the job (5 stages, the data, why it is fair — Aggregator IS both
     an AggregateFunction and a Collector, checksums asserted first).
  2. methodology, condensed to the six rules §20 states.
  3. the headline: the core-scaling table (wall/ev/s, 1/2/4/8 cores),
     already fact-checked in the three prior lanes.
  4. the full one-run table (all rows, wroclaw-table-refresh).
  5. four supporting findings, each with its own already-verified
     table: fixed cost vs marginal, the merge-parallel mechanism
     (linear-ish scaling, no shuffle), the 1-core-loses/8-core-wins
     reversal decomposed, java.util.stream's groupingBy-vs-Windowed
     story, and the 4-process distributed numbers.
  6. the Source table (already built for §20), reused verbatim.
  7. a closing pointer to docs/benchmarks.md §20 for the full
     narrative, refutations and correction history this page
     deliberately does not repeat.
  8. linked from docs/README.md's "Going deeper" and from README.md's
     Core scaling paragraph, alongside the existing §20 reference.

not done: no new measurement, no renumbering of §20, no deletion of
  anything from §20 — this is an additional entry point, not a move.

predict: every table on the new page must be a byte-for-byte or
  unit-converted copy of a table already in §20 (already fact-checked
  in scaling-table-docs/first/wall/sources); verified programmatically
  again before committing, same discipline as those four lanes.
