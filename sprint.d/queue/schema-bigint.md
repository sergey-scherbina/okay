- [ ] schema-bigint — `SBigInt` in okay-codec's `Schema` GADT and every algebra (Json, Cbor, Yaml, Xml, JsonSchema, Form, ToolSpec, Typed, Compat): stage 0 of specs/scalus.md. Cardano asset quantities are uint64 (beyond Long), minted quantities signed int64, Plutus `Data.I` unbounded; an `SIso` over String would make every consumer see text. Spark side maps it to decimal(20,0)/decimal(38,0). Operator ask 2026-09-22 (Cardano → DataFrame).
