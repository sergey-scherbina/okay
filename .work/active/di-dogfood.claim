task: di-dogfood
agent: opus5-di
started: 2026-09-09
heartbeat: 2026-09-09T17:11:15Z
status: in-progress
branch: feature/di-dogfood
worktree: ../okay-wt-dogfood
spec: specs/di.md (the arc, used by an application for the first time)

why: no application in this repository builds itself with `Module`.
  The only user outside the core's own tests is okay-deploy's
  TestNeeds, which is a test. An arc proved only by its own tests is
  not proved.

what: ChatDemo.main — today `provide(guarded(...), Secrets.env, board)`
  around a Resource region — expressed as modules, with the server as
  an acquired one; the demo's deployment needs derived from the root's
  type where the shapes fit (specs/di.md stage 3, the loop it
  promised). Behaviour unchanged, the demo's tests the guard. WHAT THE
  EXERCISE TEACHES goes into the spec: friction a test cannot show is
  the point of doing it.

next: read ChatDemo's construction and its tests, then convert
