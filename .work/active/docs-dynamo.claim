task: docs-dynamo
agent: fable-persist
started: 2026-09-09
heartbeat: 2026-09-09T06:57:41Z
status: in-progress
branch: feature/docs-dynamo
worktree: ../okay-wt-docs-dynamo
spec: specs/data.md, Behavior (new boxes under the Docs class)

why: persistence audit lane 7 of 7: Docs was designed for Dynamo/
  Cassandra/Mongo and met only Mongo; condition expressions and
  eventual reads have not tested the seam.

what: okay-docs-dynamo (JVM): DynamoDocs over the DynamoDB JSON
  protocol with SigV4 (okay-blob's signer, service "dynamodb"), Cond →
  condition expressions, declared indexes → GSIs, grants(Quorum/
  Strong) → ConsistentRead; the DocsSuite contract Live against
  dynamodb-local (docker okay-dynamo-live on 8000).

next: worktree, DynamoDocs.scala, TestDynamoDocs
