- [ ] okay2-xml — okay-codec's Xml: the lossless XML/HTML dialect, the
      NESTING prover (named tags; mismatched, missing and ancestor
      closes as error leaves; void elements; CDATA, comments;
      `render(parse(s)) == s`). Needs okay2-lex-parse. (2026-09-25)
