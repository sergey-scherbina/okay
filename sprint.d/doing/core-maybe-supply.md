- [ ] core-maybe-supply — operator ask 2026-09-26 after a core review:
      a `Maybe` effect lifted from Option, unrelated to Throws/Abort (so
      a row may hold Maybe AND Throws % E); `Either -> Throws % E` lift;
      a `Supply` effect (fresh values). Spec specs/core-gaps.md, which
      also files the rest of the review as backlog.d/okay-core items.
