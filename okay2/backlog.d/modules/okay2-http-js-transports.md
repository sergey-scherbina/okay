- [ ] okay2-http-js-transports: okay-http's Scala.js `Transports` and
      `Client` on okay2 (fetch for `Http`, the browser/Node WebSocket for
      `Sockets`), and the Scala.js acceptance run of `Acceptance.check`
      against a JVM server. okay2-http's shared half and its JVM
      transports have landed (stage 44). The JS half is filed separately
      so the route work did not wait on it. (2026-09-25)
