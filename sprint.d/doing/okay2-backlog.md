- [ ] okay2-backlog — operator 2026-09-24: "a separate backlog for okay2,
      inside". okay2/backlog.d/ as a board of its own (preamble, _order,
      sections), the okay2 items moved there by git mv, the review's
      findings filed in it; board.sh, the pre-commit hook and
      TestBoardEntries learn nested `<dir>/backlog.d` boards (shape,
      open-only, one slug in one place across ALL boards);
      backlog.d/okay2 stays as a pointer section. (2026-09-24)
