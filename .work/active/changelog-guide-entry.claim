task: changelog-guide-entry
agent: opus5-optics-outside
started: 2026-09-11
heartbeat: 2026-09-11T02:45:00Z
status: in-progress
branch: feature/changelog-guide-entry
worktree: ../okay-wt-cg

why: two bookkeeping debts from the lane that just landed.
  (1) optics-outside-guide has NO CHANGELOG entry, though the protocol
      asks for one per landed task and it is 353 lines of
      documentation.
  (2) its release-claim commit (050d87d8) cites b9671a3e, which the
      pre-gate rebase rewrote — the real commit is 3a75d36f. Caught by
      the check added to AGENTS.md an hour earlier, applied to my own
      landing. A pushed commit message cannot be edited, so the
      correction belongs in the ledger, where a reader looks.

what: the CHANGELOG entry, naming 3a75d36f and saying plainly that the
  release-claim message names a sha that was rewritten.

next: entry, gate, land
