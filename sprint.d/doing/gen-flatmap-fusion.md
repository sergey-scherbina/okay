- [~] gen-flatmap-fusion — fuse `flatMap`/`++`/`zipWithIndex` into the Gen reader (specs/gen-chain-fusion.md Out of scope, operator 2026-09-23: "сделай"): a `Cat` chain node and a `FlatMap` stage read the inner/right chain FROM the current downstream state (`readState`, `Xf.inject/project`); `zipWithIndex` a `Counted` stage. Laws: fused = materialised on random chains with all three; the nested-comprehension laziness counter; indices continue across `++`. Number: GenBenchmark lanes for flatMap/concat/zipWithIndex before/after, quiet alternated.
