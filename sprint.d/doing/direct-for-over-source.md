- [~] direct-for-over-source — `for x <- src do body` inside a `direct` block over an EFFECTFUL source (specs/direct-loops.md v3; operator "сделай", 2026-09-23): `Pull[A, G]` in core (a step as a program; `Pull.of(stream)`, `told`/`toldIn` for writer programs, `foreach` = the loop as a program outside blocks), `Take.each[I]` in okay-stream, and the macro road that emits `bind(p.step) { Some((h, tl)) => body; loop(tl) | None => pure }` — fired by the receiver's type, marks or not. Replaces the hand-written `!.loop` over `uncons`/`await` theory ch. 7 still names.
