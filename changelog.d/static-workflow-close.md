## static-workflow-close - the arc is done, and the boards say so

Housekeeping with one correction in it. `static-workflow` closed every
stage that was in scope on 2026-09-18 — the type and the laws, the
deploy check and the exhaustive cut, optics on a step and the picture —
and its notation closed with it. The queue still described stages 2-4
as what to do next, which is the same drift `queue-audit` cleaned out
of seven other items a few hours earlier; a board that names finished
work sends the next agent looking for it.

AND ONE BOX WAS STALE IN THE SPEC ITSELF: stage 5's list still carried
`proc`-notation as a gated option, with a note saying it had been moved
and ungated. It was moved, ungated AND landed in full the same day. A
line that records a decision about an item is not the same as the
item's state, and this one had been read as both.

What the arc left behind is written where the next agent will look:
stage 5's three items are gated with triggers, the deploy check is two
questions of which a term answers one, `proc-form-consumer` is the box
this arc declined to tick, and docs/static-workflows.md is the page.
