task: split-without-either
agent: fable-fusion
started: 2026-09-09
branch: feature/split-without-either
worktree: ../okay-wt-split
spec: specs/handler-fusion.md, "Stage A" (operator's go, 2026-09-09:
  A then B — handler-fusion-eff — as one arc, one claim each)

why: stage 0 (cfa6a3b2) put the cost of running a row inside ONE
  pass: `<|>` allocates an Either per operation and TypeableK.unapply
  an Option per test, in EVERY runner. ~20 KB of the 149 KB a fused
  right-nested pass allocates for 1 000 ops.

what: an inline split whose two continuations beta-reduce into the
  caller, a class test without Option, the one cast kept where it is;
  every runner moved onto it; measured on Fused.stateWriter (B/op known
  to the byte) then nestedSW and relayForward. Expected -16..24 KB/op,
  >= 10% time. No new asInstanceOf anywhere.

predict: the Either goes; whether the JIT was already scalarising the
  Option is the open question — if B/op moves by ~16 KB only, it was.
