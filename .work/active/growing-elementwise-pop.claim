task: growing-elementwise-pop
agent: opus5-bench
started: 2026-09-08
branch: feature/growing-elementwise-pop
worktree: ../okay-wt-elem

why: the last thing between here and the default the operator asked
  for. `Source.merge` reads 3.5x slower when `Channel.apply` defaults
  to `growing`, while every control is flat and `growing_chunk` — the
  same buffer with a CHUNKED consumer — is the best lane on the page.

what: find where it goes, then fix THAT.

  MY FILED HYPOTHESIS IS ALREADY WRONG and this claim starts by saying
  so. The backlog entry blamed "a part scan per pop" and suggested
  remembering the last part. `AdaptiveFifo.pop` already does both: a
  straight line when `open == 1`, and `popScanning` starting from
  `startAt`, which is exactly that memory. At two producers it scans
  at most two parts. That is not 3.5x.

  So: `-prof stack` on `okaySourceMerge` under the growing default
  against the ring, and attribute before touching anything.

predict: deliberately none. Two of my last three predictions on this
  file were wrong (the volatile, the part scan), and the one before
  that (the closing thread) too. The profile decides.
  DISQUALIFYING for any fix: `growing_chunk` must not move. It is the
  best lane on the page and it shares every line of code with the one
  being fixed.
