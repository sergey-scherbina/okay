## shift0-dollar-close - the shift0/$ arc's open questions, closed in writing

Docs-only, 2026-09-25. Three specs, one backlog item, no code.

- specs/shift0-dollar.md: stage 4 (the CPS hierarchy via APLAS 2012's
  translation) DECLINED until a consumer asks for `shift_i` — Layered
  and Lexical took `$` and need one level; a typed `control0` to a `$`
  DECLINED (stage 3 answered the one use, dollar-doors made the refusal
  a compile error in the stacked door); stacked `control0` and the
  refused ICFP 2011 example recorded as the index's known
  incompleteness, with the road (a per-continuation requirement) filed
  as backlog `stacked-k-requirements` (LOW).
- specs/layered-reflection.md: stage 3's box closed — the docs page is
  docs/direct-style.md "Layer 1½", both papers, the capabilities paper's
  example translated with `List` for their `Future`, pinned in
  TestLayered.
- specs/cont-stack.md, stage F: after C and D, re-price `stateLexDeep`
  and `stateShallow` and update docs/many-instances.md's "Choosing"
  table — deep's 4x is the capture path those stages are about.
