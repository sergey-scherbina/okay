- [ ] okay2-sql — okay-sql on okay2: `Sql` (SqlValue, SqlType,
      Isolation), `Tx`, `Row`/`Column` over Schema, `Query`, `Typed`
      (the typed layer, derivation by macro), `Pool`, `Temporal`,
      `Placeholders`; and the JDBC driver (okay-jdbc) on the JVM so it
      runs against sqlite/pg in tests. Needs okay2-codec (Schema). The
      pg wire driver (okay-pg) on demand. (2026-09-25)
